# S2G–Qwen v4：输出容量验收计划

## Material Passport

- Origin Skill: academic-research-suite / experiment-agent
- Origin Mode: run
- Origin Date: 2026-09-27
- Verification Status: 启动前冻结计划，结果另报
- Version Label: s2g-author-api-capacity8-v4

## 这一步与创新点的关系

当前是G0工程验收：获得可信、可审计的强FRESH基线。**增加输出容量不算创新。**
研究主线仍是：固定底座和预算下，历史动作是否提供强FRESH未覆盖的查询机会；若有，哪些适用条件帮助执行前选择，并减少有害复用。
本批没有历史卡，不训练选择器，不把S2G收益记给历史。阶段门槛见[研究主线](D:/project/GrowRAG/docs/experiments/2026-09-27_研究主线与阶段门槛.md)。

## 唯一新增变量

固定作者commit 5d842a67a0a99a7b545bbad0dc402ceaae0e5eff；作者提示、循环、gap拼接和抽取语义不变，strict schema与v3相同。只在API传输层更改输出容量：

| 阶段 | 作者请求 | 实际Qwen上限 |
|---|---:|---:|
| Judge | 256 | 768 |
| Extractor | 64 | 128 |
| Answer（BASE与S2G相同） | 128 | 256 |

原始requested值和actual值分别进入日志；provenance明确Qwen迁移profile。上限不是强迫模型输出相应长度。Judge/Extractor strict schema，Answer plain；temperature0、top_p1、thinking关闭；固定qwen3.7-flash-2026-07-15。

不更新schema、不重写prompt、不自动扩容、不改输出、不失败重试。原始v1/v2/v3不可覆盖。旧8题是已暴露工程题，不是独立盲测。

## 数据和对照

旧8道Hotpot官方train/debug，原每题候选池、文档级BM25、top6。BASE单次检索；S2G最多4次检索。预算不相等，非单因素有效性实验，不与论文全维基成绩比较。
官方dev/test不使用；无建库、训练、GPU、SSH。

成功条件：预定8组均完整、字段通过检查、真实usage与模型快照完整、停止有界。准确率不作为是否继续的条件。先两组检查协议再继续，首次失败立即停。已有离线合成测试执行到四轮与终止上限；不把合成多轮算成真实多轮收益。

## 预算和监控

- 新run：2026-09-27_s2g_author_api_capacity8_v4，独立claim，不能通过换目录重跑。
- 启动前核账：792请求，已知估计¥0.1281978，保守预留¥0.9709094，旧未知1次。
- 在原HISTORY_ROOTS外计入v1/v2/v3三个final_budget.json；项目累计上限¥50，本批子上限¥5。
- 最多88次理论请求，防护上限100次；最大输出额度38,912 tokens（不等于实际用量）。
- 单请求45秒；全批30分钟；输入字节上限30,000，schema正文计入预留。
- 首错、未知usage、模型快照不符、预算或时间耗尽停止，结果不补造。
- 官方价格2026-09-27再次核验：北京短上下文输入0.2、输出0.8元/百万tokens。估计不是账单。[价格源](https://help.aliyun.com/zh/model-studio/qwen3-7-flash)

## 启动与只读查看

工作目录：D:\project\GrowRAG；运行前测试与代码提交完成。

```powershell
.\.venv\Scripts\python.exe -u -m growrag.experiments.run_s2g_author_pilot --upstream external/s2g_author_snapshot/nianaaa-S2G-RAG-5d842a6 --manifest data/hotpotqa/representation_sep11_v1/manifest.json --runs-root runs --output runs/2026-09-27_s2g_author_api_capacity8_v4 --api-config qwenAPI.md --allow-network --qwen-capacity
```

另开窗口滚动查看，不触发API：

```powershell
Get-Content 'D:\project\GrowRAG\runs\2026-09-27_s2g_author_api_capacity8_v4\live.log' -Tail 20 -Wait
```

查看进程、结果：

```powershell
$s2gRun = Get-Content 'D:\project\GrowRAG\runs\2026-09-27_s2g_author_api_capacity8_v4\process.json' -Raw | ConvertFrom-Json
Get-Process -Id $s2gRun.pid
Get-Content 'D:\project\GrowRAG\runs\2026-09-27_s2g_author_api_capacity8_v4\summary.json' -Raw
```

程序退出后PID不存在正常，不把旧PID当仍运行。启动命令不要为了看结果重跑。

## 通过以后做什么

先整理每题错误发生在查询、原文到达、证据保留、Judge或答案哪一层，再准备新的32题共享小语料诊断。共享语料需要独立manifest审计，不能偷用gold筛文档；本计划不自动启动32题。
如果失败，先报告原因，不在同批里改协议。本轮最多这一个新的付费协议。

