# DualRAG：重点段落中英精读

更新：2026-09-24。

## 这篇最需要学会什么

1. 查询不是独立改一句话：推理发现缺口，查询围绕实体展开，证据按实体积累。
2. 学清楚无训练 DualRAG 与微调版 DualRAG-FT 的区别。我们先做前者的有界适配。
3. 作者已经把跨问题知识复用写入未来方向。GrowRAG 不能只说“给它加长期记忆”就声称新颖。

论文：Rong Cheng, Jinyi Liu, Yan Zheng, Fei Ni, Jiazhen Du, Hangyu Mao, Fuzheng Zhang, Bo Wang, Jianye Hao. *DualRAG: A Dual-Process Approach to Integrate Reasoning and Retrieval for Multi-Hop Question Answering*. ACL 2025 主会长文，31877–31899。

[正式版本](https://aclanthology.org/2025.acl-long.1539/) · [PDF](https://aclanthology.org/2025.acl-long.1539.pdf) · [作者代码](https://github.com/cbxgss/rag)

许可与版本：选段来自正式 ACL PDF，采用 [CC BY 4.0](https://aclanthology.org/faq/copyright/)，去除了排版断词。中文为本项目非官方翻译；解释与建议为我们的分析，不代表原作者。本文件不是全文翻译。PDF页从1开始。

## 1. Abstract（PDF 1 / 31877）

**英文原文**

> Multi-Hop Question Answering (MHQA) tasks permeate real-world applications, posing challenges in orchestrating multi-step reasoning across diverse knowledge domains. While existing approaches have been improved with iterative retrieval, they still struggle to identify and organize dynamic knowledge. To address this, we propose DualRAG, a synergistic dual-process framework that seamlessly integrates reasoning and retrieval. DualRAG operates through two tightly coupled processes: Reasoning-augmented Querying (RaQ) and progressive Knowledge Aggregation (pKA). They work in concert: as RaQ navigates the reasoning path and generates targeted queries, pKA ensures that newly acquired knowledge is systematically integrated to support coherent reasoning. This creates a virtuous cycle of knowledge enrichment and reasoning refinement. Further, through targeted fine-tuning, DualRAG preserves its sophisticated reasoning and retrieval capabilities in smaller-scale models, demonstrating its versatility and core advantages across different scales. Extensive experiments demonstrate that this dual-process approach substantially improves answer accuracy and coherence, approaching, and in some cases surpassing, the performance achieved with oracle knowledge access. These results establish DualRAG as a robust and efficient solution for complex multi-hop reasoning tasks.

**中文翻译**

多跳问答任务广泛存在于真实应用中，对跨越不同知识领域组织多步推理提出了挑战。现有方法虽借助迭代检索得到改善，但在识别和组织动态知识方面仍有困难。为此，我们提出 DualRAG：一个协同的双过程框架，将推理与检索紧密结合。DualRAG 通过两个紧耦合过程运行：推理增强查询（RaQ）和渐进式知识聚合（pKA）。二者协作：RaQ 沿推理路径生成有针对性的查询，pKA 则系统地整合新获得的知识以支持连贯推理，从而形成知识丰富与推理改进的良性循环。此外，通过有针对性的微调，DualRAG 在较小规模模型上也保留了复杂推理和检索能力，表现出跨模型规模的适用性和核心优势。大量实验表明，这一双过程方法明显改善答案准确性和连贯性，接近、并在部分情况下超过使用理想知识访问条件时的表现。这些结果支持 DualRAG 作为复杂多跳推理任务中稳健、高效解决方案的有效性。

**不要直接接受的营销式读法**

- “在部分情况下超过 oracle”不表示超越信息理论上限。这里 oracle 是论文设置的对照输入，具体输入可能不完整或不利于模型使用，必须查看定义。
- “高效”要看检索次数、生成token、延迟和模型大小，不能只看最终正确率。
- 论文 Table 2：72B下 Hotpot F1，IRCoT为67.4，DualRAG为65.7；7B下分别52.8与58.6，DualRAG-FT为61.6。因此它不是在每个设置都领先。

## 2. Introduction：静态检索为什么不够（PDF 1 / 31877）

**英文原文**

> Traditional RAG methods follow a retrieve-then-read paradigm, where documents are retrieved based on the original query and then used for answer generation (Gao et al., 2024). While effective for simple tasks, such a fixed retrieval strategy struggles to adapt flexibly to the ever-changing knowledge demands when confronted with multi-hop questions.

**中文翻译**

传统 RAG 遵循先检索后阅读的范式：根据原问题检索文档，再利用文档生成答案。这种固定检索策略虽然对简单任务有效，但面对多跳问题时，难以灵活适应不断变化的知识需求。

**与我们的关系**：前置q-only层便宜，但看不到第一轮发现的新实体。PRE 和 POST 不是谁绝对先进，而是决策时可见信息不同。应比较“便宜但信息少”和“贵但信息多”的成本—效果关系。

## 3. Introduction：信息越多不一定越好（PDF 2 / 31878）

**英文原文**

> As retrieval demands increase, a third core challenge arises: how to efficiently organize and utilize the retrieved information. Noise in retrieved documents, stemming from both the documents themselves and retrieval tools, is a common issue (Cai et al., 2024; Xu et al., 2024; Chen et al., 2024b; Yoran et al., 2024). In iterative RAG, noise accumulation can progressively interfere with the model’s understanding of available knowledge. Moreover, poor organization of complex documents fragments knowledge, making it challenging for the model to construct a coherent reasoning chain (Liu et al., 2024; Agrawal et al., 2023). Existing works have attempted to address this issue through re-ranking (Li, 2023; Jiang et al., 2023a, 2024), yet overlook the inherent associations among different documents.

**中文翻译**

随着检索需求增加，第三个核心挑战出现了：如何高效组织和利用检索信息。检索文档中的噪声是常见问题，它既可能来自文档本身，也可能来自检索工具。在迭代 RAG 中，噪声积累会逐渐干扰模型对已有知识的理解。此外，复杂文档若组织不佳，知识就会碎片化，使模型难以建立连贯的推理链。已有工作尝试通过重排序处理这一问题，却忽视了不同文档之间内在的关联。

**我们的分析**：重排序决定哪些片段先看；知识组织决定跨片段的关系怎样保留。这两个模块不能混为一谈。历史经验同样会带来额外噪声，所以需要记录“相似但不适用”的反例，不是只给相似卡片加top-k。

## 4. 方法图应该怎样读

```text
原问题 + 本题知识提纲
        ↓
Reasoner：还缺什么，是否继续检索？
        ↓ 需要
Entity Identifier：围绕实体生成针对性query
        ↓
Retriever + reranker：取得并筛选相关文档
        ↓
Knowledge Summarizer：按实体组织知识、更新本题提纲
        └────────────→ 下一轮Reasoner
        ↓ 不再需要 / 达到轮次上限
Answer：结合知识作答
```

这是方法逻辑，不是新的实验图。原方法默认最多5轮。我们当前开发适配明确限制2轮、每轮实体/查询数量，并暂用候选池BM25；这些都是差异，不偷换为论文原版。无长期跨题知识写入。

## 5. Conclusion：全文（PDF 9 / 31885）

**英文原文**

> We propose DualRAG, a dual-process RAG framework that tightly integrates Reasoning-augmented Querying (RaQ) and progressive Knowledge Aggregation (pKA) to address multi-hop QA problems. RaQ dynamically identifies knowledge demands and formulates targeted retrieval queries, while pKA structures and refines acquired information to support coherent reasoning. Meanwhile, by fine-tuning DualRAG on smaller models, we ensure that it maintains its strong reasoning and retrieval capabilities, allowing it to retain its core advantages even with reduced resource consumption. Experimental results on multiple datasets demonstrate that DualRAG significantly improves answer accuracy and coherence, confirming its effectiveness as a robust and efficient solution for complex reasoning tasks.

**中文翻译**

我们提出 DualRAG，一种把推理增强查询和渐进式知识聚合紧密结合的双过程 RAG 框架，用来处理多跳问答问题。RaQ 动态识别知识需求并形成针对性的检索查询；pKA 则组织、提炼已经获得的信息，支持连贯推理。同时，通过对较小模型进行微调，DualRAG 在降低资源消耗的情况下仍保留较强的推理和检索能力。多个数据集上的实验表明，它明显改善了答案准确性和连贯性，支持了其作为复杂推理任务中稳健、高效方法的有效性。

**学会分开两个贡献**：组织检索流程；将这一流程的能力蒸馏到小模型。复现前者不强制训练后者。GrowRAG首轮不需要把这两项一起做。

## 6. Limitations：完整段落（PDF 9 / 31885）

**英文原文**

> Despite the effectiveness of our approach, several limitations remain. First, while our method exhibits some robustness to noise, real-world retrieval corpora may still suffer from missing, contradictory, or outdated knowledge, which can impact reasoning reliability. To address these challenges, future improvements could involve enhancing the Knowledge Summarizer to better resolve inconsistencies and infer missing information. Second, although our method reduces retrieval frequency by identifying knowledge needs, thereby minimizing interruptions to reasoning, it still introduces additional latency due to multi-turn retrieval. A promising direction for improvement is to enhance the Knowledge Outline, enabling it to progressively accumulate and reuse knowledge across questions, thereby reducing retrieval dependency on multi-turn retrieval and facilitating continuous learning and self-improvement.

**中文翻译**

尽管本方法有效，仍存在若干局限。首先，虽然方法对噪声具有一定鲁棒性，真实检索语料仍可能存在知识缺失、互相矛盾或过时的问题，影响推理可靠性。为应对这些挑战，未来可以增强知识摘要器，使其更好地解决不一致并推断缺失信息。其次，虽然通过识别知识需求，本方法降低了检索频率，减少了对推理的打断，多轮检索仍带来额外延迟。一个有希望的改进方向是增强知识提纲，使其能够跨问题逐步积累和复用知识，从而降低对多轮检索的依赖，支持持续学习和自我改进。

**作者意见与我们的选择要分开**

- 作者提出：增强知识摘要器，处理缺失、矛盾、过时知识。
- 作者提出：跨问题积累知识，降低多轮检索依赖。
- 我们暂不把推断出来的缺失信息当成真实文档事实。推测与来源支持必须分开记录。
- 我们更偏向复用**修复程序及其适用条件**，不是存储历史答案；这与跨问题知识提纲有区别，但程序性记忆也已有 AWM、RRM、ReasoningBank 等先例，差异不等于自动新颖。

## 7. Future work：完整两段（PDF 9 / 31885）

**英文原文，第1段**

> In future work, we plan to incorporate reinforcement learning to further enhance DualRAG’s performance. Reinforcement learning can enable the system to better optimize retrieval and reasoning strategies through continuous feedback, improving adaptability and efficiency in complex tasks.

**中文翻译，第1段**

未来，我们计划引入强化学习，进一步提升 DualRAG 的表现。通过持续反馈，强化学习可以让系统更好地优化检索与推理策略，提高其在复杂任务中的适应性和效率。

**英文原文，第2段**

> We also aim to apply DualRAG in AI for Science, where structured retrieval and reasoning are crucial. For example, in biological sciences, frameworks like CellAgent (Xiao et al., 2024) highlight the potential of LLMs in automating scientific workflows. Similarly, in scientific domains such as software testing (Zheng et al., 2019) and tokamak plasma control (Degrave et al., 2022), DualRAG can complement deep reinforcement learning by organizing domain knowledge and guiding exploration.

**中文翻译，第2段**

我们也希望将 DualRAG 应用于科学智能领域，那里结构化检索与推理十分关键。例如，生物科学中的 CellAgent 等框架展示了语言模型自动化科学工作流的潜力。类似地，在软件测试、托卡马克等离子体控制等领域，DualRAG 可以通过组织领域知识、引导探索，与深度强化学习形成互补。

**本项目不立即做**：没有必要把“RL+跨任务+科学领域”同时打包。一个清楚可反驳的条件表示实验，比一个没有归因的复杂系统更适合现在。

## 8. Discussion 应读的实验证据

正式版没有独立 Discussion 标题。读 §4.5 两组消融：删流程组件和取消对应组件的微调。Table 4中去掉EI微调时，Hotpot某些指标没有降低；但Figure 4讨论查询冗余减少。说明“模块使系统更省”与“模块使EM更高”是两个不同主张。

阅读 §4.7 时要继续翻 Appendix F 的完整案例，检查模型发现的缺口是否由证据产生，不只看作者挑出的成功结论。

## 9. 阅读检查题

1. Reasoner与Entity Identifier为什么不合成同一个角色？拆开多花几次API是否值得？
2. Knowledge Outline与S2G的Evidence Context有什么不同？前者整理知识，后者保留指向原句的证据。
3. 如果摘要器把两个同名人合并，后续再好的query能否修复？
4. 哪个消融能区分“结构好”与“模型更强”？
5. 本轮小候选池适配，哪些实验结论不能推广到fullwiki？

个人阅读笔记：

- 我不理解的术语：
- 我认为不充分的实验：
- 我愿意复现的一个模块：
