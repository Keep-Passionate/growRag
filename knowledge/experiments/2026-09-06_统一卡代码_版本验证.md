# 统一经验卡：版本与验证记录

日期：2026-09-06。只记录工程事实，不报告新的模型效果。

## 版本

- 开发分支：`codex/query-card-actions-v1`，从旧 Hotpot pilot 分支延续，不改 main。
- 前一阶段检查点：`ff1da96`，保存外循环、独立 QPP 诊断、此前图稿及文档。
- 本轮功能提交：`4a25352b00b20536fdde85a8e1be437ba70e3fb3`，统一外壳／不同正文、多来源只读视图、版本化改写入口和专项测试。
- 包版本：0.2.0；研究快照标签 `v0.2.0-research.1` 已推送，远端解析后对应上述功能提交。
- 功能分支已经成功推送，远端读取确认其当时 HEAD 等于 `4a25352`。后续本记录的提交不改变功能代码。

## 实际运行验证

1. 当前工作区完整测试：424 passed；本轮新增 42 项 typed card 专项测试。
2. 从已提交的 `4a25352` 导出干净源代码，明确将导出目录的 src 作为导入路径，在临时目录完整运行：424 passed。
3. 两处静态检查通过，84 个 Python 文件格式检查通过。
4. 干净副本没有本地 qwenAPI、私有配置、Hotpot 原始数据、模型权重或 runs；测试使用自己的合成 fixture，不依赖这些未提交文件。
5. 使用当前已有 Python 环境执行干净副本测试，不等同于在全新系统重新安装全部依赖。GitHub CI 配置会另外在 Python 3.11／3.12 上安装 `[dev,qpp]` 并检查。
6. 对拟提交路径和高置信密钥模式作检查；没有提交密钥、data/runs/models，不使用强推，不自动合并主分支。

GitHub 功能提交 CI：[运行记录](https://github.com/Keep-Passionate/growRag/actions/runs/34039319001)。已核验 completed／success；Python 3.11 和 3.12 两个作业均成功。标签在该功能提交通过 CI 后建立并推送，不只依据本地测试。

## 审计与结论边界

独立审计指出 typed JSON 重复键可能导致程序只读最后一个值，而模型看到歧义文本。本轮对任意层级重复键显式拒绝，补充顶层／正文／条件三组回归测试。

所有模型交互专项测试使用显式 mock transport。没有调用付费 API、没有新 LLM 改写结果、没有重新训练。421／424 等测试数量不是答案正确率；也不能将先前 QPP 的 21/16 小样本结果算作本轮新算法效果。

当前价值是让同一张卡的操作形式、来源、阶段和传输内容可追溯且一致。自动判断经验是否适用于当前题、是否优于 FRESH，仍需下一阶段配对数据及真实模型检验。
