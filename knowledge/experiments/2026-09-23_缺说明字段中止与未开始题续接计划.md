# 非关键说明缺字段：中止与人工审计后的未开始题续接

> Material Passport
> - Origin Skill: experiment-agent
> - Mode: run
> - Date: 2026-09-23
> - Verification status: UNVERIFIED（已检查首批实际日志；本续接尚未执行）
> - Version: bounded-system-unstarted-v2

## v1 实际发生了什么

启动代码 b02f887、clean；原8题只开始第000题。20次真实API响应均返回固定Qwen快照，估算0.005361元、预留0.0403954元。

BASE1、FRESH1、REFLECTIVE2、ADAPTIVE_NO_MEMORY2 完成；ADAPTIVE_MEMORY2 第二轮判断缺少非控制说明字段 reason，被原严格解析器拒绝。S2G_GAP2 尚未执行。整批停止，剩7题没有开始，六臂完整题数0。

被拒响应的 requirements / sufficient=false / useful_gain=null / gap / next_intent 都存在；缺的不是充分性或证据ID，而是额外说明。这是已检查原JSON后的结论，不把所有格式错误一律称非关键。

两条无记忆二轮路径确实发现 Jet Jongeling 出生于 Delft，但仍缺 Delft 属哪个省的证据。没有伪装答案正确或记忆收益；该题记忆策略实际选BASE、第二轮FRESH，0次REUSE。

另一个关键观察：三个相同第二轮 Reader 请求的 request_sha256 一致，却得到空答、Delft、South Holland。temperature=0 不等于服务完全确定。正式归因必须共享相同输入的执行结果或采用预注册重复，不能将这种差异算成历史效果。

## 现在修什么、不修什么

- 新增 ASSESS_PARSER_VERSION=v2，只允许 reason 缺失，写明确本地占位说明及 normalization 标签；原响应保留。
- 提示词、模型、所有控制字段/证据ID约束、六方法、候选库、轮数/证据预算不变。
- 旧run/claim/budget/report全部不改；不把旧失败标成成功、不补造 S2G 结果。
- 旧20请求不重试，不重跑第000题，也不拿该题的 South Holland 生成新卡。

## 显式续接未开始题，不是重跑失败请求

独立人工审计入口 `--continue-unstarted-from runs/2026-09-23_bounded_system_8_v1`，严格检查旧计划、prompt、model、来源和数据指纹一致，以及已开始目录与前缀题号一致，只返回原序列中**从未开始的001—007七题**。不能任意选择哪些题跳过。

新代码先本地验收并提交；旧20调用账本纳入累计：627次、已知估算0.0951174元、保守预留0.7535982元、1历史费用仍未知。新子限3元仍位于累计50元内。

只跑未开始题，每题六臂。新数据单独保存为 `runs/2026-09-23_bounded_system_unstarted7_v2`，原第000题保持不完整的诊断状态。七题结果单独报告，不补成8题分母。

如再次出现核心字段/调用错误，则停止，不自动进入第三批、不换模型、不重复付费请求。下一步依据日志诊断，不盲目扩大试验。
