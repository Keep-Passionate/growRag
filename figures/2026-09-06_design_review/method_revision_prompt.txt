Edit this exact attached GrowRAG method diagram and preserve the entire content, typography and panel layout. Correct only the following connector and legend issues. (1) Remove all three long blue horizontal connectors from individual numbered steps in left panel (a) into the table: their current row alignment is misleading. Replace them with ONE short blue rightward arrow in the whitespace gap between the outer borders of panel (a) and panel (b), halfway down their height, indicating that the WHOLE procedure is checked against the current state. It must not originate from any specific numbered step or suggest Step 3 maps to Step 2. (2) The teal REUSE-ready row must feed the query box q1 ONLY. The current horizontal teal line forks downward into both q1 and Same retriever R: REMOVE the downward branch that bypasses q1 and goes straight into R. Preserve exactly this execution chain: ready row -> q1 -> Same retriever R -> New current evidence E1. Preserve the gray dashed recheck loop. There must be no bypass into the retriever and no outgoing execution arrow from WAIT. (3) Replace the bottom-right legend text "Dashed amber: proposed" with "Entire method: proposed study" inside the same small box, because the whole method is under discussion, not just the amber note. Keep the top subtitle indicating implementation and novelty pending. (4) In the bottom shared-state box, change wording to "Same q0, E0, model and budget" on its first two lines and a smaller third line "Memory fixed across reuse variants". The FRESH branch receives no history. Do not add any scores, fictional returned evidence, or other changes. White background and readable text.
