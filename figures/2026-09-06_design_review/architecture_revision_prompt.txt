Edit the attached GrowRAG architecture draft, preserving its typography, canvas, modules, layout, and all other labels. Correct a scientifically critical arrow error: the line from Frozen memory currently ends at Reader; this is WRONG. Completely remove that vertical memory-to-Reader line and its arrowhead. Frozen memory must have exactly ONE outgoing route, which ends at Candidate retrieval, not at Reader and not at the online output. Route it from Frozen memory down to the whitespace above the dashed freeze boundary, then horizontally left to above Candidate retrieval, then down into the TOP of Candidate retrieval. Place the label "procedures only" along this corrected elbow connector. Do not obscure the offline nodes, the freeze-boundary label, or the repair-layer title; adjust whitespace or the layer title position slightly if necessary. There must be NO direct or indirect factual evidence connector from Frozen memory to Reader. Keep BASE bypass carrying E0 to Reader, and keep Current evidence feeding Reader; these are Reader's only evidence inputs. Also make the Current state connection visibly branch into Candidate retrieval and Evidence-aware selection; use a small junction to avoid dangling arrows at the repair container edge. Preserve all other content. This is a design draft, not an empirical result. White background, sharp legible text.
