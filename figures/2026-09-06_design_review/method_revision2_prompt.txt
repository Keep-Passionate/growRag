Make a very small connector cleanup to the attached diagram. Preserve every panel, word, table row, number, color, and layout unless explicitly stated here. In panel (b), REMOVE the long teal line that leaves the right edge of the REUSE-ready row and goes down into the top of the New current evidence E1 box. Also REMOVE the bent teal line above the q1 and Same retriever R boxes that currently makes a backward R-to-q1 loop. Do NOT replace either removed line with a new route. The only teal connectors remaining in this part of panel (b) must be TWO short horizontal arrows, left-to-right: q1 -> Same retriever R -> New current evidence E1. The decision table has NO outgoing connector at all. Put a small plain teal text label just above the q1 box reading "Instantiate the READY step"; this textual label replaces the need for a connector from the table. Keep the gray dashed evidence recheck loop at the far right. Keep the single blue arrow between panels (a) and (b), and the offline panel (c) unchanged. All other content must remain identical. No new arrows, no other changes. Remove the unwanted connector pixels completely on the white background, leaving clean whitespace.
