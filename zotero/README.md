# Zotero 文件

- `curated/`：2026-09-05 精选整合包，当前优先入口；20 篇，含逐篇中文导读子笔记与阅读顺序。
- `current/`：历史分批推荐和增量 RDF，保留追溯；这次无需重复逐个导入。
- `legacy/`：旧导入包，仅用于查历史，可能与当前包重复。

导入新 RDF 前建议在 Zotero 中建立独立 collection；如出现重复条目，按 DOI 或 arXiv ID 合并。
