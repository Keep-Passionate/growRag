# 2026-09-24 FRESH 基线阅读草稿状态

本轮曾准备以 2023 Query2doc/IRCoT 为主的 `records.json`，但用户随后追加“近期 CCF A 主会/顶刊”标准。该未发布草稿已移除，**不是当前选定基线或阅读顺序**。本目录没有生成/导入新的 RDF。

最新筛选说明见：`docs/GrowRAG_顶会FRESH基线筛选_2026-09-24.md`。S2G-RAG（ACL2026）与 DualRAG（ACL2025）为候选；前者需要训练判断器，后者有免微调版本但不是纯前置改写层。实际采用和复现范围仍须区分，不能将我们的 Qwen 简化版冒充原方法。

旧导入包与用户阅读批注未修改。此标记保留草稿来源，避免将它误认为已确认的“最新主基线”。
