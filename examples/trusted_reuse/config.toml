[experiment]
name = "trusted_reuse_file_demo"
seed = 42

[actions]
direct = true
reuse = true
fresh = false

[evaluation]
good_threshold = 0.5
max_history_candidates = 5
paired_harm_is_primary = true

[environment]
corpus_id = "toy-wiki"
corpus_version = "2026-08"
retriever_id = "bm25"
retriever_version = "1"
index_id = "toy-index"
index_version = "sha256:demo"
analyzer_id = "unicode-words"
analyzer_version = "1"
rewriter_id = "precomputed-plan"
rewriter_version = "v1"

[write_gate]
minimum_source_gain = 0.0

[reliability]
minimum_transfer_observations = 3
minimum_direct_good_observations = 2
minimum_benefits = 1
minimum_mean_gain = 0.0
maximum_conditional_breakage_upper_bound = 0.70
quarantine_at_harm_count = 1

[candidate_recall]
scorer = "source-query-jaccard-v1"
maximum_candidates = 5

[applicability]
scorer = "signature-coverage-v1"
minimum_score = 0.50

[application]
applier_id = "precomputed-plan"
application_version = "v1"

[budget]
maximum_retrieval_queries = 1
top_k = 5
context_tokens = 4096
maximum_query_characters = 2048
